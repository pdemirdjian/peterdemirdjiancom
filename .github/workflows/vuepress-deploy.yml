name: Build Vuepress
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Generate Site
        uses: actions/setup-node@v2
        with:
          node-verion: '16'
      - run: npm ci
      - run: npm run build